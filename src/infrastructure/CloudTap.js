class CloudTap {
  constructor(id, waitingSetup, remainingVolume) {
    this.id = id;
    this.waitingSetup = waitingSetup;
    this.remainingVolume = remainingVolume;
  }
}

export default CloudTap;
